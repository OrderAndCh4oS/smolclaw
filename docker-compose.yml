services:
  smolclaw:
    build: .
    ports:
      - "18789:18789"
    env_file: .env
    volumes:
      - ./store:/app/store
      - ./cache:/app/cache
      - ./logs:/app/logs
      - ./sessions:/app/sessions
      - ./memory:/app/memory
